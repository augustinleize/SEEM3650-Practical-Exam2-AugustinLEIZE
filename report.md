
# SEEM3650 - Practical Exam Report
**Name:** Augustin LEIZE
**Student ID:** 1155264529

## Task 2: Shakespeare Character-level Model
Below are the first 5 lines generated by my model after 500 iterations : 

All before will and is be be made
To bear to take and my calleans barth
The us crock. What that a enaw crown.

GREMIO:
Go must of the of it heart milend like eart well.

## Task 3: Model Architecture Exploration

### Validation Loss Comparison
Based on my Student ID (ending is 529), my remainder for Task 3 is **1** (529 mod 4 = 1)

| Number of Layers (`n_layer`) | Final Validation Loss (at step 500) |
| :--- | :---|
| 2 | 1.8393 |
| 3 | 1.7955 |
| 5 | 1.7146 |
| 7 | 1.6969 |


* **Fixed Parameter:** Number of attention heads (`n_head`) = 5.
* **Variable Parameter:** Number of layers (`n_layer`) ∈ {2, 3, 5, 7}.
* **Embedding Dimension:** `n_embd` was adjusted to 380 to be divisible by the 5 heads.
* **Training Iterations:** 500 per configuration.
* ## Visualization

The relationship between model depth and performance :

![Validation Loss Plot](figures/loss_plot.png)

### Conclusion & Best Settings
* **Lowest Validation Loss:** 1.6969
* **Best Configuration:** `n_layer = 7` and `n_head = 5`

**Observation:** The results indicate that increasing the number of layers consistently improves the model's predictive capabilities, as the validation loss decreased with every layer added.

---

## Task 4: Training BabyGPT for Code Generation

### Dataset Preparation
* **Language:** Python (as Student ID 529 mod 2 = 1).
* **Source:** Python code collected from GitHub (user flask)
* **Token Count:** 156.011  tokens.

### Model Configuration
A specialized configuration was created in `config/train_code_generation.py` using the best architecture found in Task 3 (`n_layer=7`, `n_head=5`).

### Generated Code Samples
Below are the first 20 lines of code generated by the model after training on the Python dataset:
```python

Overriding: out_dir = out-code-python
number of parameters: 12.17M
Loading meta from data/code_generation/meta.pkl...

                   def checks.Error(
                           "'%s % name field." % (dif model_name, clash.name, "dataabels)
                        obj=cls,
                         id="models.E016",
                       )
                    elif lde `= field.name`
                   errors.append(
                    checks.Error(
                            "'This to cann the field '%s' "
                        "'order mimuttion for than list o long." % unique_for field_names
          
---------------

from django.db.models.utils import NON_PSION_SERT_PREBURE_ROT"] as colections and elie[bool:
       """
   def _check_field_name__ = self.__get___(self.__self)
       return inserted_fields_map_labels)
```
This snippet demonstrates the model's ability to replicate Python's structure, specifically function definitions and returns:
```python
def _check_field_name__ = self.__get___(self.__self)
       return inserted_fields_map_labels)
```


The model successfully decreased the validation loss to 1.4444 in 500 iterations. Despite the character-level limitations, the model effectively learned Python indentation and common keywords like def, return, and import.




